version: "3"
services:
  db:
    image: postgres
    container_name: postgres
    restart: always
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: simpleblog
      POSTGRES_USER: usr
      POSTGRES_PASSWORD: pwd
